# ROLLBACK RUNBOOK — video-pipeline checkpoint 2026-07-26 ~04:00
State: first fully-successful end-to-end 12s/3-chunk Kira video (filed 03:59).

## Mac warm server (no rebuild needed)
1. launchctl bootout gui/$(id -u)/com.barkadabrew.comfybox
2. cp ~/.comfybox/checkpoints/20260726-video-pipeline/com.barkadabrew.comfybox.plist ~/Library/LaunchAgents/
3. cp ~/.comfybox/checkpoints/20260726-video-pipeline/ComfyBox ~/Projects/zimage.swift/.build/release/ComfyBox
   cp ~/.comfybox/checkpoints/20260726-video-pipeline/mlx.metallib ~/Projects/zimage.swift/.build/release/
   xattr -cr ~/Projects/zimage.swift/.build/release/ComfyBox && codesign --force --sign - ~/Projects/zimage.swift/.build/release/ComfyBox
4. launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.barkadabrew.comfybox.plist

## Source rollback (if rebuilding)
- zimage.swift: git checkout checkpoint-video-20260726  (= dde3932; branch claude/ltx2-haze-optimization)
- Weights: ~/LocalModels/sexgod-distill06-bf16 (mirror: /Volumes/Bolt/Models/sexgod-distill06-bf16)

## Box daemon (coffeeshop-server)
- Tag checkpoint-video-20260726 = 179e6b5 (pushed to origin).
- ssh todd@10.0.100.232 (FISH shell): cd ~/coffeeshop-server; and git checkout checkpoint-video-20260726; and node build.mjs; and systemctl --user restart kira-daemon

## Kira config
- Snapshot: kira-config.json (this dir) -> ~/.kira/config.json on box, then restart kira-daemon.

## Verify after rollback
- curl -s http://127.0.0.1:7870/health (Mac, LAN-bound 0.0.0.0)
- Kira scheduler: curl -s http://localhost:3787/v1/kira/content-scheduler/status (paused:false)
- One anchor render scores ~sharp 35/flicker 0.24 (qa/video/metrics.py vs qa assets).
