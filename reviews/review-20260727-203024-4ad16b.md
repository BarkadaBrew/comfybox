# Review — tiered scheduler v2 (loop 20260727-203024-4ad16b)

Two independent Codex reviews ran against coffeeshop-server (the stop-hook's
own attempt exited 1 in this repo, which holds no diff for this task; the
reviews below were run manually with `codex exec` against the actual branch,
`claude/kira-tiered-scheduler`).

## Round 1 — design review (pre-implementation)

Verdict: BLOCK (4 blockers, 7 warns). All resolutions folded into the spec
(`docs/superpowers/specs/2026-07-27-kira-tiered-scheduler-design.md`, commit
703cb29b): behavior-preserving migration keyed by the content-mode store's
current default; strict PUT semantics (mixed payloads 400, tiers = full
replacement, flat = legacy patch); per-tier activeHours key required;
`modeSource` field (no DaemonEvent.source collision); nullable effectiveMode
status shape; immediate-tick notifier for overrides; live interval reschedule;
profile-allowlist + Telegram-allowlist integration gates; scheduler tier
dialect (no apple↔neutral canonicalization); options-object suggestion API;
zero-floor chain budget.

## Round 2 — implementation review (post-build)

Verdict: REQUEST CHANGES → addressed in d5fab5aa:

1. BLOCKER — flat GET/PUT hardcoded tiers.avocado; an old client's stepper tap
   could conjure a 24/7 avocado tier that wins every cycle. Fixed:
   `legacyProjectionTier()` — flat clients see and edit avocado-if-present,
   else the migrated tier; flat-created tiers start on the overnight default.
2. WARN — malformed stored activeHours failed the GET→PUT round-trip. Fixed:
   `sanitizeTier()` on read.
3. WARN — integration gates untested. Fixed: real-file stream-mode tests,
   end-to-end tick precedence + poke-past-gate, /stream command handler +
   allowlist assert, set_stream_tier execute + profile allowlist assert.

## Final state

- Typecheck clean; all 113 kira test files pass; full suite shows only the
  3 known pre-existing failures (tile compositor + 2 shell-hook tests).
- Ready for the server merge gate + kira-update deploy + desktop redeploy.
